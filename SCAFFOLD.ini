PulsOS/
├── README.md
├── SCAFFOLD.ini
├── CMakeLists.txt
├── memory.md                       <- session context for AI-assisted development
├── docs/
│   └── ARCHITECTURE.md
├── resources/
│   └── fonts/
│       └── .gitkeep
├── vendor/
│   ├── cimgui/                     <- Dear ImGui source (git submodule) -- cimgui bindings NOT used
│   └── implot/                     <- ImGui-native charts (git submodule)
└── src/
    ├── main.c
    ├── core/
    │   ├── process.h               <- ProcessInfo, Snapshot, AppState, TreemapNode, ProcessHistory
    │   ├── poll.h
    │   └── poll.c                  <- kqueue event loop, GCD dispatch, double buffer, ring buffer
    ├── platform/
    │   ├── proc_platform.h         <- shared signature: proc_get_snapshot()
    │   ├── proc_macos.c            <- [done] libproc backend
    │   ├── proc_linux.c            <- [todo] stub (/proc filesystem)
    │   └── proc_windows.c          <- [todo] stub (WMI / NtQuerySystemInformation)
    ├── gpu/
    │   ├── metal_context.h
    │   ├── metal_context.m         <- MTLDevice, MTLCommandQueue, MTLBuffer setup
    │   └── treemap.metal           <- color mapping kernel (cpu% -> heat color) -- GPU path pending
    └── ui/
        ├── app.h
        ├── app.cpp                 <- [done] SDL2 init, ImGui context, DPI scaling, main loop
        ├── process_list.h
        ├── process_list.cpp        <- [done] sortable table (PID, CPU%, MEM, Name) + selector
        ├── detail_panel.h
        ├── detail_panel.cpp        <- [done] per-PID info row + sparkline scaffold (history todo)
        ├── treemap.h
        ├── treemap.cpp             <- [done] squarified layout + HSV colors + hit test + Metal dispatch
        ├── implot_wrapper.h        <- extern "C" sparkline API (keeps implot out of C files)
        └── implot_wrapper.cpp      <- [done] only file that includes implot.h