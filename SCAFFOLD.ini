PulsOS/
├── README.md
├── SCAFFOLD.ini
├── CMakeLists.txt
├── docs/
│   └── ARCHITECTURE.md
├── resources/
│   └── fonts/
│       └── .gitkeep
├── vendor/
│   ├── cimgui/                 ← Dear ImGui C bindings (git submodule)
│   └── implot/                 ← ImGui-native charts (git submodule)
└── src/
    ├── main.c
    ├── core/
    │   ├── process.h           ← ProcessInfo, Snapshot, AppState — the contract
    │   ├── poll.h
    │   └── poll.c              ← kqueue event loop, GCD dispatch, double buffer, ring buffer
    ├── platform/
    │   ├── proc_platform.h     ← shared signature: proc_get_snapshot()
    │   ├── proc_macos.c        ← ✅ libproc backend
    │   ├── proc_linux.c        ← 🔲 stub
    │   └── proc_windows.c      ← 🔲 stub
    ├── gpu/
    │   ├── metal_context.h
    │   ├── metal_context.c     ← MTLDevice, MTLCommandQueue, MTLBuffer setup
    │   └── treemap.metal       ← color mapping kernel (cpu% → heat color)
    └── ui/
        ├── app.h
        ├── app.c               ← SDL2 init, ImGui context, FSM, main loop
        ├── process_list.h
        ├── process_list.c      ← sortable table + process selector
        ├── detail_panel.h
        ├── detail_panel.c      ← per-PID sparkline + info (implot)
        ├── treemap.h
        └── treemap.c           ← squarified layout CPU + dispatches Metal render pass